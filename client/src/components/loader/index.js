import { LoaderDiv, Scanner } from "./styles";

const Loader = () => {
  return (
    <LoaderDiv>
      <Scanner>
        <h1>Loading...</h1>
      </Scanner>
    </LoaderDiv>
  );
};

export default Loader;
